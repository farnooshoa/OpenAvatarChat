log:
  log_level: "INFO"

service:
  host: "0.0.0.0"
  port: 7860
  cert_file: null
  cert_key: null

default:
  chat_engine:
    model_root: "models"
    handler_search_path: ["src/handlers"]
    handler_configs:
      SileraVad:
        module: vad/enhanced/vad_handler_adaptive
        speaking_threshold: 0.5
        start_delay: 2048
        end_delay: 2048
        buffer_look_back: 1024
        speech_padding: 512
        adaptive_threshold: true
        noise_gate_db: -40
        dynamic_sensitivity: true
      
      AudioProcessor:
        module: audio/enhanced/audio_handler_noise_reduction
        enable_noise_reduction: true
        target_wer: 0.10
        spectral_subtraction: true
        wiener_filtering: true
      
      ASR_Funasr:
        module: asr/sensevoice/asr_handler_sensevoice
        model_name: "iic/SenseVoiceSmall"
      
      LLMGroq:
        module: llm/openai_compatible/llm_handler_openai_compatible
        model_name: "llama-3.1-70b-versatile"
        system_prompt: "You are an AI avatar assistant with enhanced speech recognition. Respond concisely and naturally."
        api_url: "https://api.groq.com/openai/v1"
        api_key: "${GROQ_API_KEY}"
      
      BargeInHandler:
        module: interrupt/interrupt_handler_bargein
        max_latency_ms: 300
        interrupt_threshold: 0.6
        enable_seamless_transition: true
        queue_management: "priority"
      
      TTS_EdgeTTS:
        module: tts/edgetts/tts_handler_edgetts
        voice: "en-US-JennyNeural"